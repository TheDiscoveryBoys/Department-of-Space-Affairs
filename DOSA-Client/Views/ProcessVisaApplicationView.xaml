<UserControl IsVisibleChanged="UserControl_IsVisibleChanged"
             x:Class="DOSA_Client.Views.ProcessVisaApplicationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:DOSA_Client.Controls"
             mc:Ignorable="d"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">

    <Grid Background="#ECEFF1">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <!-- Content -->
            <RowDefinition Height="Auto"/>
            <!-- Buttons -->
        </Grid.RowDefinitions>

        <!-- Header Ribbon -->
        <controls:HeaderRibbon DataContext="{Binding}" />

        <!-- Content -->
        <Grid Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="30,10,30,20">
            <Border Background="White"
                    CornerRadius="10"
                    Padding="40"
                    VerticalAlignment="Stretch">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Visa Application Review"
                                   FontSize="22"
                                   FontWeight="Bold"
                                   Margin="0,0,0,20"/>

                        <Grid Margin="50,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.5*"/>
                                <ColumnDefinition Width="3*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Applicant Info -->
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" FontWeight="Bold" Margin="0,10"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding VisaApplication.Applicant.Name}" Margin="0,10"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Email:" FontWeight="Bold" Margin="0,10"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding VisaApplication.Applicant.Email}" Margin="0,10"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Species:" FontWeight="Bold" Margin="0,10"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding VisaApplication.Applicant.Species}" Margin="0,10"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Planet of Origin:" FontWeight="Bold" Margin="0,10"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding VisaApplication.Applicant.PlanetOfOrigin}" Margin="0,10"/>

                            <!-- Application Info -->
                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Destination Planet:" FontWeight="Bold" Margin="0,10"/>
                            <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding VisaApplication.VisaApplication.DestinationPlanet}" Margin="0,10"/>

                            <TextBlock Grid.Row="5" Grid.Column="0" Text="Travel Reason:" FontWeight="Bold" Margin="0,10"/>
                            <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding VisaApplication.VisaApplication.TravelReason}" Margin="0,10"/>

                            <TextBlock Grid.Row="6" Grid.Column="0" Text="Start Date:" FontWeight="Bold" Margin="0,10"/>
                            <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding VisaApplication.VisaApplication.StartDate}" Margin="0,10"/>

                            <TextBlock Grid.Row="7" Grid.Column="0" Text="End Date:" FontWeight="Bold" Margin="0,10"/>
                            <TextBlock Grid.Row="7" Grid.Column="1" Text="{Binding VisaApplication.VisaApplication.EndDate}" Margin="0,10"/>
                        </Grid>

                        <Grid Margin="40,20">
                            <TextBox Name="ReviewerNotesTextBox"
                                     Height="100"
                                     FontSize="14"
                                     Padding="10"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True"
                                     VerticalScrollBarVisibility="Auto"
                                     Background="#FAFAFA"
                                     BorderBrush="#B0BEC5"
                                     BorderThickness="1"/>

                            <TextBlock Text="Enter any relevant notes or instructions here..."
                                       Foreground="Gray"
                                       Margin="14,12,0,0"
                                       FontSize="14"
                                       IsHitTestVisible="False"
                                       VerticalAlignment="Top"
                                       x:Name="PlaceholderTextBlock"/>

                            <!-- Visibility binding for placeholder -->
                            <Grid.Triggers>
                                <EventTrigger RoutedEvent="TextBox.TextChanged" SourceName="ReviewerNotesTextBox">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="PlaceholderTextBlock"
                                                       Storyboard.TargetProperty="Visibility">
                                                <DiscreteObjectKeyFrame KeyTime="0:0:0">
                                                    <DiscreteObjectKeyFrame.Value>
                                                        <Visibility>Collapsed</Visibility>
                                                    </DiscreteObjectKeyFrame.Value>
                                                </DiscreteObjectKeyFrame>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Grid.Triggers>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Buttons -->
        <Grid Grid.Row="2" Margin="30,0,30,20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Reject"
                        Command="{Binding RejectCommand}"
                        Margin="0,0,30,0"
                        Width="150"
                        Height="40"
                        Background="Red"
                        Foreground="White"
                        FontWeight="Bold"
                        FontSize="16"
                        Style="{StaticResource RoundedButtonStyle}"/>
                <Button Content="Approve"
                        Command="{Binding ApproveCommand}"
                        Width="150"
                        Height="40"
                        Background="#7F5AF0"
                        Foreground="White"
                        FontWeight="Bold"
                        FontSize="16"
                        Style="{StaticResource RoundedButtonStyle}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>

<UserControl x:Class="DOSA_Client.Views.ProcessPassportApplicationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:DOSA_Client.Controls"
             mc:Ignorable="d"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">

    <Grid Background="#ECEFF1">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <controls:HeaderRibbon DataContext="{Binding}" />

        <!-- Content -->
        <Grid Grid.Row="1" Margin="30,10,30,20">
            <Border Background="White" CornerRadius="10" Padding="40">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Passport Application Review" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

                        <Grid Margin="50,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.5*"/>
                                <ColumnDefinition Width="3*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Applicant Info -->
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" FontWeight="Bold" Margin="0,10"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding PassportApplication.Applicant.Name}" Margin="0,10"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Email:" FontWeight="Bold" Margin="0,10"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding PassportApplication.Applicant.Email}" Margin="0,10"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Species:" FontWeight="Bold" Margin="0,10"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding PassportApplication.Applicant.Species}" Margin="0,10"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Planet of Origin:" FontWeight="Bold" Margin="0,10"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding PassportApplication.Applicant.PlanetOfOrigin}" Margin="0,10"/>
                        </Grid>

                        <!-- Document Section -->
                        <TextBlock Text="Uploaded Documents" FontSize="18" FontWeight="SemiBold" Margin="0,30,0,10"/>
                        <ItemsControl ItemsSource="{Binding PassportApplication.Documents}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding FileName}"
                                            Command="{Binding DataContext.DownloadDocumentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Margin="0,5"
                                            Style="{StaticResource RoundedButtonStyle}"
                                            HorizontalAlignment="Left"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Buttons -->
        <Grid Grid.Row="2" Margin="30,0,30,20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Reject"
                        Command="{Binding RejectCommand}"
                        Margin="0,0,30,0"
                        Width="150"
                        Height="40"
                        Background="Red"
                        Foreground="White"
                        FontWeight="Bold"
                        FontSize="16"
                        Style="{StaticResource RoundedButtonStyle}"/>
                <Button Content="Approve"
                        Command="{Binding ApproveCommand}"
                        Width="150"
                        Height="40"
                        Background="#7F5AF0"
                        Foreground="White"
                        FontWeight="Bold"
                        FontSize="16"
                        Style="{StaticResource RoundedButtonStyle}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
